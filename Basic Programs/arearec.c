// to calculate area of the rectangle
#include<stdio.h>
void main(){
	float l,w,area;
	printf("Enter the length and width of rectangle:");
	scanf("%f %f", &l , &w);
	area = l*w;
	printf("The area of the reactangle is: %f",area);
}
